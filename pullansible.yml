---
- name: Firswt demo ansible playbook
  hosts: localhost
  connection: local
  gather_facts: yes

  tasks:
   - name: print hello world
     debug:
      msg: "Hello Universe"

   - name: print host name
     debug:
      var: ansible_hostname

   - name: print ubuntu version
     debug:
      var: ansible_distribution
